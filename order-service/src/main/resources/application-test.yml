server:
  port: 20002

spring:
  profiles:
    active: test
  thymeleaf:
    cache: false
    encoding: 'UTF-8'
    mode: 'HTML5'
  datasource:
    url: 'jdbc:mysql://10.40.253.187:4306/xgqoms?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&tinyInt1isBit=false&allowMultiQueries=true'
    username: 'root'
    password: '123456'
    driver-class-name: 'com.mysql.jdbc.Driver'
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      #最大连接数
      max-active: 20
      #最小连接数#
      minIdle: 5
      #获取连接最小等待时间（毫秒）
      maxWait: 10000
      #解决Mysql 8小时断开空闲连接的问题
      validationQuery: SELECT 'x'
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 30000


mybatis:
  mapper-locations: 'classpath*:/Mapper/*.xml'

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:2000/eureka

