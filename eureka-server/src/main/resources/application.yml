server:
  port: 2000

eureka:
  instance:
    host-name: icecreamGroupEurekaServer
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:2000/eureka